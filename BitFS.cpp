#include <iostream>
#include <vector>
using namespace std;
/*
** bit全探索の雛形

*/

/* Debug */
int main(){

    int K; //組み合わせの長さ

    for(int bit=0; bit<(1<<K); bit++){
        vi vec(N, 0);
        REP(i, 0, K){
            if(bit & (1<<i)){
                /*1の場合の処理*/
            }
        }
    }
    return 0;
}

